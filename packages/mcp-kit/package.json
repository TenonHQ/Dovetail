{
  "name": "@tenonhq/dovetail-mcp-kit",
  "version": "0.0.1",
  "engines": {
    "node": ">=22"
  },
  "description": "Shared MCP tool-registration kit for Dovetail MCP servers — annotation presets, the actionable+retryable error contract, JSONL telemetry, and registerTool plumbing (incl. outputSchema → structuredContent).",
  "main": "./dist/index.js",
  "scripts": {
    "test": "jest",
    "prepack": "tsc -p tsconfig.build.json",
    "version:bump": "node ./../../Scripts/bump-version.js ./package.json",
    "postpublish": "npm run version:bump"
  },
  "author": "Multiple",
  "license": "GPL-3.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": ">=20.8.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TenonHQ/Dovetail.git",
    "directory": "packages/mcp-kit"
  },
  "bugs": {
    "url": "https://github.com/TenonHQ/Dovetail/issues"
  }
}
